// Type definitions converted to JavaScript
// These are for reference only - JavaScript doesn't enforce types at runtime

// Tier values that can be used for validation
export const TIER_VALUES = ['Bronze', 'Silver', 'Gold', 'Diamond'];

// Tier type for reference (used in TypeScript files)
// In JavaScript, you can use: const tier = 'Bronze'; // Must be one of TIER_VALUES

// Note: Interfaces and types are removed as they don't exist in JavaScript
// If you need type checking, consider using JSDoc comments or PropTypes
// or convert the consuming files to TypeScript

